//changing colors, clearing canvas, saving canvas
//changing stroke weight
//------------
//a pattern brush
//frame count for gradient
//randomness for stochastic effects
//at least one GUI object like a button
//drawing app

function setup() {
  createCanvas(640, 480);
  if {(createCanvas(640, 480);}
}

function draw() {
// if (mouseIsPressed) {
//    fill(0);
//  } else {
//    fill(255);
//  }
//  ellipse(mouseX, mouseY, 80, 80);
}

function mouseDragged() 
{ 
	strokeWeight(10);
	line(mouseX, mouseY, pmouseX, pmouseY);
}